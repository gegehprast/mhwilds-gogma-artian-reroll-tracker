export type BonusKey = "bonus1" | "bonus2" | "bonus3" | "bonus4" | "bonus5"
export type BonusData = Record<BonusKey, string>

export const BONUS_KEYS: BonusKey[] = [
  "bonus1",
  "bonus2",
  "bonus3",
  "bonus4",
  "bonus5",
]
